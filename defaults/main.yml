trickster_version: 1.0.1
trickster_user: trickster_sv
trickster_cli_flags: {}
trickster_listen_port: 9099
trickster_listen_address: 0.0.0.0
trickster_logging_log_level: info
trickster_metrics_listen_port: 8082
trickster_metrics_listen_address: 127.0.0.1
trickster_origins:
  - origin: default
    config:
      - is_default = true
      - origin_type = 'prometheus'
      - origin_url = 'http://localhost:9090'
trickster_caches:
  default:
    config:
      - cache_type = 'memory'
